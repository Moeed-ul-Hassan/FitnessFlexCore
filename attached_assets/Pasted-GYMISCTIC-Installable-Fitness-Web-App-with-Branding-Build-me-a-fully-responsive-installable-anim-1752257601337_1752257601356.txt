GYMISCTIC – Installable Fitness Web App with Branding
Build me a fully responsive, installable, animated, and modern fitness web app named GYMISCTIC using React + Tailwind CSS for frontend and FastAPI (Python) or Node.js (Express) for backend. It should work like a Progressive Web App (PWA) and follow the feature breakdown below:

🧩 User Features
1. 🏋️ Workout Planner
Let users create weekly workout routines with muscle group targeting.
Each day should allow:

Muscle selection (Chest, Back, Legs, etc.)

Sets, reps, and rest time

Save and view weekly plans

Option to use pre-made plans: Beginner / Intermediate / Advanced
Use reusable WorkoutCard components.

2. 🥗 Diet Planner (Budget Based)
Allow users to:

Select goal (Weight Loss / Muscle Gain / Maintenance)

Select budget (Low / Medium / High)

Get filtered meals with:

Meal timing (Breakfast, Lunch, etc.)

Calories, protein, carbs, fats breakdown

Budget per day estimate
Use MealCard components with clean layout.

3. 📈 Progress Tracker
Users can:

Input weight, height, BMI weekly

View visual charts using Chart.js

Animated progress bar for goal achievement (e.g., 45% to goal)
Create components like ProgressChart and GoalBar.

4. 🔔 Motivation & Reminders
Show daily motivational quote (random from predefined list)

Display toast reminder if user skips routine
Use react-toastify or a modal system with framer-motion.

5. 📊 Personalized Dashboard
A dashboard showing:

Today’s Workout Summary

Today’s Meal Plan

Progress & goal status
Use grid layout with animated widgets.

6. 🔐 Authentication
Use Firebase Auth or Supabase Auth

Email/password + Google login

Auth-protected routes
Each user sees their own saved routines, meals, and stats.

7. 🧑‍💻 Admin Panel (Optional)
Admin login for:

Uploading/editing workouts and meals

Viewing user activity

Secure route with role check

8. 📄 PDF Report Generator
On dashboard, include button:

"Download Monthly Report"
Generate PDF using html2pdf or jsPDF with user’s:

Progress stats

Diet summaries

Workout logs

🛠️ Tech Stack Requirements
Frontend: React + Tailwind CSS

Backend: FastAPI or Node.js

Database: Supabase or SQLite

Charts: Chart.js

Auth: Firebase or Supabase

PDF: jsPDF

Deployment: Netlify (frontend), Render/Vercel/Railway (backend)

🎨 UI/UX Guidelines
Dark theme with modern fitness UI

Colors:

Background: #0f172a

Primary: #10b981 (Neon Green)

Accent: #60a5fa (Sky Blue)

Text: #ffffff

Fonts: Use Google Fonts – Inter or Poppins

Buttons:

Class: px-4 py-2 rounded-2xl text-white bg-green-500 hover:bg-green-600 shadow-md

Animations using framer-motion: hover scale + subtle fade in

All pages fully responsive and mobile-first

🪟 Install Prompt – Add to Home Screen (PWA)
Add manifest.json with:

json
Copy
Edit
{
  "name": "GYMISCTIC",
  "short_name": "GYMISCTIC",
  "start_url": "/",
  "display": "standalone",
  "theme_color": "#0f172a",
  "background_color": "#0f172a",
  "icons": [
    {
      "src": "/icons/icon-192x192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-512x512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
Show popup when beforeinstallprompt is fired:
Message:

🚀 Install GYMISCTIC for faster access.
Buttons: Install Now / Maybe Later
On click, run prompt() and hide popup.

Register a service worker to enable offline access.

🌊 Branding Watermark
Add a persistent, subtle watermark on every screen:

html
Copy
Edit
<div className="fixed bottom-2 right-2 text-white text-xs opacity-40 z-50 pointer-events-none">
  Made by Moeed ul Hassan 🚀
</div>
Make sure watermark is always visible but doesn’t block content.

📁 Deliverables
Full working GitHub repo

Deployed demo (Netlify/Vercel)

README with setup guide

manifest.json + service-worker.js

Icons in /public/icons/ folder

